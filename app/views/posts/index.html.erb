<%= link_to 'Listar todos los post', '/', class: "boton-posts"%>

<%= link_to 'Listar todos los post', '/', remote: true%>

<div class="row">
  <div class="col-lg-6">
    <%= render partial: 'form'%>
  </div>
  <div class="col-lg-6">
    <table class="table">
      <thead>
          <th>Título</th>
          <th>Descripción</th>
      </thead>
        <tbody>
          <!-- <% @posts.each do |post|%>
              <tr>
                <td><%= post.title%></td>
                <td><%= post.description%></td>
              </tr>
            <% end %> -->
        </tbody>
    </table>
  </div>
</div>

<script>
  PostIndexView()
</script>
